<%= image_tag "index.png" %>
<%= link_to button_tag("Logout"), logout_path %>
<%= link_to image_tag("index.png"), home_profile_path%>
<section>
  <h1>My pinwall</h1>
  <p>Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonoumy eirmod tmpor invidunt ut labore et dolore magna aliquyam erat, sed diam cpluputa. At vero eos et accusam et just.</p>
</section>
<section>
  <% @pictures.each do |p| %>
  <section>
<!--   Insert the Picture.image.url-->
    <article>
      <%= image_tag "index.png" %>
      <%= @likes.where(picture_id: p.id).count %>
    </article>
    <article>
      <% @comments.each do |c| %>
      <article>
        <% user = @users.find(c.user_id) %>
<!--        Insert the comments User image.url-->
        <% if c.picture_id == p.id %>
          <p><%= c.comment %></p>
        <% end %>
      </article>
     <% end %>
      <article>
<!--        insert the image of the current_user-->
        <%= form_for :comment do |nc| %>
          <% nc.text_field :user_id, :input_html => { :value => @current_user.id} %>
          <% nc.text_field :picture_id, :input_html => { :value => p.id} %>
          <%= nc.text_field :comment %>
          <% nc.submit %>
        <% end %>
      </article>
    </article>

    <%=  %>
  </section>
  <% end %>
</section>